def dynamic_weighted_a_star(grid_size, start, goal, poisons):
    """
    Dynamic Weighted A* que devuelve un camino como lista de posiciones [(fila,col)].
    Por ahora es solo un ejemplo con un camino fijo.
    """
    # TODO: implementar la lógica real
    path = [(0,0), (0,1), (1,1), (2,1), (3,1), (4,1), (4,2), (4,3), (4,4)]
    return path
