def beam_search(grid_size, start, goal, poisons, beam_width=2):
    """
    Beam Search que devuelve un camino como lista de posiciones [(fila,col)].
    Por ahora es solo un ejemplo con un camino fijo.
    """
    # TODO: implementar la lógica real
    path = [(0,0), (1,0), (2,0), (3,0), (4,0), (4,1), (4,2), (4,3), (4,4)]
    return path
